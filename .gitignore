# NativeScript
hooks/
node_modules/
platforms/
dist/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# General
.DS_Store
.AppleDouble
.LSOverride
.idea
.cloud
.project
tmp/
typings/
.gradle-cursor
__pycache__/
*.py[cod]
.review-*/
App_Resources/Android/src/main/jniLibs/**/*.so
App_Resources/Android/src/main/assets/faceclaw-voice-asr/

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# pull_config.sh output: the phone's live settings, including API keys
faceclaw_settings.xml
hermes_g2_settings.xml
App_Resources/Android/whatsapp-node/node_modules/

# Session scratchpad: pulled device DBs, screenshots, ground-truth dumps (never commit)
scratchpad/

# Secrets and private data (never commit). Operational identifiers + credentials
# live in a local secrets file; the raw captures, firmware, and API tokens live
# in ../ground-truth-private (outside this repo). Belt-and-suspenders: ignore that
# path too in case it is ever copied inside the tree.
secrets.local.md
secrets.local
.env.local
*.secret
.even-jwt
ground-truth-private/
